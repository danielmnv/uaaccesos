import 'package:flutter/material.dart';

class ColorPalette {
  // static Color primary = Color(0xff003375);
  static Color primary = Colors.blue[900];

  // static Color secondary = Color(0xffde0059);
  static Color secondary = Colors.red;

  // static Color accent = Color(0xfff8b323);
  static Color accent = Colors.yellow[600];

  // static MaterialColor
}
